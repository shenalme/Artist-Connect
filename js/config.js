// Paste your Supabase project details here (Dashboard > Project Settings > API).
// The anon/publishable key is designed to be public: the database's Row Level
// Security rules decide what each visitor can do. NEVER put the service_role
// (secret) key in this file.
export const SUPABASE_URL = 'https://YOUR-PROJECT-REF.supabase.co';
export const SUPABASE_ANON_KEY = '';
